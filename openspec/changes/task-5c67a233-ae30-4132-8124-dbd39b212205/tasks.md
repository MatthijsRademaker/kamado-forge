# Implementation Tasks

- [ ] 1. Resolve and record the canonical session-plan contract/readiness matrix: authoritative schema location and generated type, date representation, duration unit/range and timing authority, temperature value/unit/ranges, stable identity needs, exact required fields, and typed empty-draft representation. Do not begin fixture or Plan UI implementation until this gate is settled.
- [ ] 2. Add the contract-owned session-plan schema without adding an endpoint; regenerate the canonical OpenAPI and frontend client artifacts through the repository command, and verify generated drift checks pass without manual generated-file edits.
- [ ] 3. Add pure, unit-tested Plan model functions for timeline offsets/totals, readiness errors and first-invalid paths, and safe nested add/remove/move operations over explicit array order.
- [ ] 4. Add a contract-typed fixture registry for complete, incomplete, and empty draft payloads plus discriminated loading/error states; implement documented `?fixture=complete|incomplete|empty|loading|error` selection, deep-cloned local state, and deterministic local reset/retry/create/return behavior.
- [ ] 5. Extend the existing pathname-based app composition with direct `/plan` handling while preserving root and `/showcase`; add the ordered fixed Today/Plan/Coach/Learn/Logbook shell with Plan active and no Vue Router or unsupported product flows.
- [ ] 6. Build the Plan editor from reusable primitives with all scoped fields, nested add/remove/move controls, derived timing, reactive summary and field validation, first-invalid focus, and neutral local ready/complete status.
- [ ] 7. Implement planned/manual target presentation and the responsive outdoor hierarchy: desktop timeline with targets/setup, mobile readiness and targets before labeled collapsible timeline and setup/vent sections, persistent bottom navigation, visible focus, 44px targets, non-color-only status, and no 320px page overflow.
- [ ] 8. Add automated frontend and browser coverage for direct route/refresh behavior, every fixture state, local creation/editing, nested add/remove/reorder boundaries, timing derivation, validation/error relationships/focus, keyboard operation, planned-target semantics, refresh reset, and 320px layout.
- [ ] 9. Update affected project documentation with the contract boundary, route-thin Plan composition, fixture selector, and local-only lifecycle semantics; run the sanctioned frontend checks and `scripts/precommit-run` successfully.
