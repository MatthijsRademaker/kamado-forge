---
description: resolve one framework-prepared git merge conflict
---

# Resolve merge conflict

Resolve only conflicted paths named in dossier below. Preserve stated intent
and dependency authority.

You are explicitly authorised to run only these git command forms:

- `git status --short`
- `git diff -- <conflicted-path>`
- `git checkout --ours -- <conflicted-path>`
- `git checkout --theirs -- <conflicted-path>`

Edit only conflicted paths named in dossier. Remove conflict markers and leave
resolved files unstaged for framework verification.

Do NOT run `git add`, commit, push, pull, fetch, merge, rebase, reset, checkout
branches, create or update pull requests, execute implementation plan, re-plan,
or perform unrelated work.

$ARGUMENTS
